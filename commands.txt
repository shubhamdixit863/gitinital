git init

git add filename  // git add . // all the files
git config --global user.name "Saqlain"
git config --global user.email johndoe@example.com

git status

git commit -m "message" // it actually saves your changes in your local git repository
git remote add origin remote_repo_url

git push -u origin master
git branch


//pulll


// branching 
//merge conflict

//reinitialization
